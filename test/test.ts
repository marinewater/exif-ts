import { assert } from 'chai';
import { equalArrayBuffer } from './helpers/equal_arraybuffer';

import { getBinaryData } from '../index';

describe( 'a', function() {

    it( 'getBinaryData', function() {

        assert.isTrue( true );

    } );

} );
